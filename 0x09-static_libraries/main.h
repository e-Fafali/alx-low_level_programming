/**
 * main.h header files
 */

/* A fuction to print  */
int _putchar(char c);

/* A function to check the case of the alphabet */
int _islower(int c);

/* A function to check for alphabet */
int _isalpha(int c);

/* A function to return the absolute the value of a number */
int _abs(int n);

/* A function to check the case of the alphabet */
int _isupper(int c);

/* A function to check for digit */
int _isdigit(int c);

/* A functio to check the lenght of a string */
int _strlen(char *s);

/*_A function */
void _puts(char *s);

/* A function to copy strings */
char *_strcpy(char *dest, char *src);

/* A function to  */
int _atoi(char *s);

/* A function to  */
char *_strcat(char *dest, char *src);

/* A function to */
char *_strncat(char *dest, char *src, int n);

/* A function to */
char *_strncpy(char *dest, char *src, int n);

/* Afunction to  */
int _strcmp(char *s1, char *s2);

/* A fuction to  */
char *_memset(char *s, char b, unsigned int n);

/* A function to */
char *_memcpy(char *dest, char *src, unsigned intn);

/* A function to */
char *_strchr(char *s, char c);

/* A function to */
unsigned int _strspn(char *s, char *accept);

/* A function to */
char *_strpbrk(char *s, char *accept);

/* A function to */
char *_strstr(char *haystack, char *needle);

/* END */
