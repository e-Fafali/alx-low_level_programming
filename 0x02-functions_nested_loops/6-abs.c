/* #include <stdio.h> */
#include "main.h"

/**
  * _abs - prints the absolute value of a number
  * @n: stores the value of the input
  * Return: -1 or 0 or 1
 */

int _abs(int n)
{
	int x;

	x = 0 - 1;

	if (n < 0)
	{
		n = n * x;
	}
	return (n);
}
