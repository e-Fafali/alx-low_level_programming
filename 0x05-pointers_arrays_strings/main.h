/**
 * Header  file
 */

/* A function to reset a number */
void reset_to_98(int *n);

/* A function to swap the values of two numbers */
void swap_int(int *a, int *b);

/* A function that returns the lenght of a given string */
int _strlen(char *s);
