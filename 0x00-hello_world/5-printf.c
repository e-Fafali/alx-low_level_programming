#include <stdio.h>

/**
 * main - Main code
 *
 * printf: a function that outputs stuff to the terminal
 *
 * Return: Always (0) (Success)
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
